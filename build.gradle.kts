// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    alias(libs.plugins.android.application) apply false // Manter esta linha
    // Remover a linha 'id 'com.android.application' version '8.8.0' apply false'
}